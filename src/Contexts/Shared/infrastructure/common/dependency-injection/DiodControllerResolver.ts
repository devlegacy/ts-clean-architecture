// import { Service } from 'diod'

import { ControllerResolver } from '.'

export const DiodControllerResolver: ControllerResolver = (_controller) => {
  // Service()(controller)
  // This is our instantiated class
  // return container?.get(controller)
}
