services:
  # http://localhost:15672/
  rabbitmq:
    container_name: rabbitmq
    image: rabbitmq:management-alpine
    ports:
      - 5672:5672
      - 15672:15672
    restart: always

networks:
  app-network:
    external: true
# docker-compose -f docker-compose.rabbitmq.yml up -d
# node --run command:mooc:rabbitmq
# node --run command:backoffice:rabbitmq
